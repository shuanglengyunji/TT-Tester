name: CI

on:
  [push]

env:
  CARGO_TERM_COLOR: always

jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Install library
      run: sudo apt-get install -y libudev-dev socat
    - name: Create virtual serial ports
      run: tools/create_serial_port_pair.sh &
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: RUST_BACKTRACE=1 cargo test --verbose -- --test-threads 1
